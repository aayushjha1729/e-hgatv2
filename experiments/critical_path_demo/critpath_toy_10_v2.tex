% Auto-generated by scripts/run_critical_path_demo.py -- do not edit by hand.
\begin{table}[t]
\centering
\caption{Worked-example critical-path traversal (R2). For the makespan-optimal Pareto schedule of each stub instance, the exact max-plus critical path is listed as an ordered sequence of binding activities whose durations sum to $C_{\max}$ (decomposition exact by construction). The fused-GNN TAPE recovers the identical path (leg-critical Jaccard) and predicts each on-path duration.}
\label{tab:critpath}
\small
\begin{tabular}{llrrr}
\toprule
Instance & On-path activities ($\to$) & $C_{\max}$ & Path len & Leg-Jaccard \\
\midrule
toy:10 & $t_{0}$:empty $\to$ $t_{0}$:loaded $\to$ $t_{5}$:empty $\to$ $t_{5}$:loaded $\to$ $t_{4}$:empty $\to$ $t_{4}$:loaded $\to$ $t_{8}$:empty $\to$ $t_{8}$:loaded $\to$ $t_{1}$:empty $\to$ $t_{1}$:loaded $\to$ $t_{7}$:empty $\to$ $t_{7}$:loaded & 454.17 & 12 & 1.00 \\
\bottomrule
\end{tabular}
\end{table}
